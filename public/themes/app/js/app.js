const menuButton=document.querySelector('.customer-menu-toggle');const backdrop=document.querySelector('.customer-backdrop');function closeMenu(){document.body.classList.remove('customer-menu-open');menuButton?.setAttribute('aria-expanded','false')}menuButton?.addEventListener('click',()=>{const open=!document.body.classList.contains('customer-menu-open');document.body.classList.toggle('customer-menu-open',open);menuButton.setAttribute('aria-expanded',String(open))});backdrop?.addEventListener('click',closeMenu);document.addEventListener('keydown',event=>{if(event.key==='Escape'&&document.body.classList.contains('customer-menu-open')){closeMenu();menuButton?.focus()}});document.querySelector('.customer-sidebar nav')?.addEventListener('click',event=>{if(event.target.closest('a'))closeMenu()});
